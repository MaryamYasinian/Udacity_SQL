SELECT *
FROM orders
WHERE gloss_amt_usd >= 1000
LIMIT 5;

SELECT *
FROM orders
WHERE total_amt_usd < 500
LIMIT 10;

IMPORTANT POINT: single quote for non-numeric data
SELECT name, website, primary_poc
FROM accounts
WHERE name LIKE 'Exxon Mobil'
